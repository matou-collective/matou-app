# KERI test network ports (infrastructure/keri with .env.test, offset +1000 from dev)
VITE_KERIA_ADMIN_URL=http://localhost:4901
VITE_KERIA_BOOT_URL=http://localhost:4903
VITE_KERIA_CESR_URL=http://localhost:4902
VITE_CONFIG_SERVER_URL=http://localhost:4904
# Tell config server to use test config file (isolated from dev)
VITE_TEST_CONFIG=true
# Backend API (test server on port 9080)
VITE_BACKEND_URL=http://localhost:9080
# Schema server URL as seen by KERIA inside Docker (same service name, namespaced by project)
VITE_SCHEMA_SERVER_URL=http://schema-server:7723
