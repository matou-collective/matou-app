# MATOU Frontend Environment Configuration (Development)
# Copy to .env or .env.local for local development

# KERIA URLs (dev defaults shown)
VITE_KERIA_ADMIN_URL=http://localhost:3901
VITE_KERIA_BOOT_URL=http://localhost:3903
VITE_KERIA_CESR_URL=http://localhost:3902

# Config server (stores org config after setup)
VITE_CONFIG_SERVER_URL=http://localhost:3904

# Backend API (Go server with any-sync + KERI bridge)
VITE_BACKEND_URL=http://localhost:8080

# Docker-internal URLs (used when passing OOBIs to KERIA)
# These are the service names as seen from within the Docker network
VITE_KERIA_DOCKER_CESR_URL=http://keria:3902
VITE_SCHEMA_SERVER_URL=http://schema-server:7723
VITE_WITNESS_DOCKER_URL=http://witness-demo:5642
