# MATOU Frontend Environment Configuration
# Copy to .env or .env.local for local development
#
# The frontend fetches all infrastructure URLs (KERIA, witnesses, anysync)
# from the config server. Only these variables are needed:

# Environment: dev | test | prod
VITE_ENV=dev

# Config server URLs (infrastructure configuration)
VITE_DEV_CONFIG_URL=http://localhost:3904
VITE_TEST_CONFIG_URL=http://localhost:4904
VITE_PROD_CONFIG_URL=https://config.matou.nz

# Backend API URL (Go server - can vary per session)
VITE_BACKEND_URL=http://localhost:4000

# API token for the backend's TokenGuard (mutating requests). Dev/test default
# is the fixed constant below; the packaged app receives a random per-launch
# token over IPC instead.
VITE_API_TOKEN=matou-dev
