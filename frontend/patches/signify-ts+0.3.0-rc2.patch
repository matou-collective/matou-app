diff --git a/node_modules/signify-ts/dist/keri/app/clienting.js b/node_modules/signify-ts/dist/keri/app/clienting.js
index 739d22e..7ddd6d8 100644
--- a/node_modules/signify-ts/dist/keri/app/clienting.js
+++ b/node_modules/signify-ts/dist/keri/app/clienting.js
@@ -272,13 +272,23 @@ export class SignifyClient {
      * @returns {Promise<Response>} A promise to the result of the rotation
      */
     async rotate(nbran, aids) {
+        // Perform the rotation first (mutates controller.signer to new key)
         const data = this.controller.rotate(nbran, aids);
-        return await fetch(this.url + '/agent/' + this.controller.pre, {
+        // KERIA processes the rotation event BEFORE verifying HTTP signature,
+        // so it expects the request signed with the NEW (post-rotation) key.
+        this.authn = new Authenticater(this.controller.signer, this.agent.verfer);
+        const path = '/agent/' + this.controller.pre;
+        // Sign manually — cannot use this.fetch() because KERIA's response
+        // may have a different signify-resource after agent rotation.
+        const headers = new Headers();
+        headers.set('Signify-Resource', this.controller.pre);
+        headers.set(HEADER_SIG_TIME, new Date().toISOString().replace('Z', '000+00:00'));
+        headers.set('Content-Type', 'application/json');
+        const signed = this.authn.sign(headers, 'PUT', path);
+        return await fetch(this.url + path, {
             method: 'PUT',
             body: JSON.stringify(data),
-            headers: {
-                'Content-Type': 'application/json',
-            },
+            headers: signed,
         });
     }
     /**
diff --git a/node_modules/signify-ts/dist/keri/app/controller.js b/node_modules/signify-ts/dist/keri/app/controller.js
index 8fdfbbe..0191367 100644
--- a/node_modules/signify-ts/dist/keri/app/controller.js
+++ b/node_modules/signify-ts/dist/keri/app/controller.js
@@ -179,13 +179,17 @@ export class Controller {
             .signers.pop();
         this.keys = [this.signer.verfer.qb64, signer === null || signer === void 0 ? void 0 : signer.verfer.qb64];
         this.ndigs = [new Diger({}, this.nsigner.verfer.qb64b).qb64];
+        const sn = new CesrNumber({}, undefined, this.serder.sad['s']).num + 1;
         const rot = rotate({
             pre: this.pre,
             keys: this.keys,
             dig: this.serder.sad['d'],
+            sn: sn,
             isith: ['1', '0'],
             nsith: '1',
             ndigs: this.ndigs,
+            cuts: [],
+            adds: [],
         });
         const sigs = [
             signer === null || signer === void 0 ? void 0 : signer.sign(b(rot.raw), 1, false, 0).qb64,
diff --git a/node_modules/signify-ts/dist/keri/core/eventing.js b/node_modules/signify-ts/dist/keri/core/eventing.js
index 80b628a..2a92e62 100644
--- a/node_modules/signify-ts/dist/keri/core/eventing.js
+++ b/node_modules/signify-ts/dist/keri/core/eventing.js
@@ -152,8 +152,8 @@ export function rotate({ pre = undefined, keys, dig = undefined, ilk = Ilks.rot,
         bt: _toad && intive && _toad !== undefined && _toad <= MaxIntThold
             ? _toad
             : _toad.toString(16),
-        br: cuts,
-        ba: adds,
+        br: _cuts,
+        ba: _adds,
         a: data != undefined ? data : [],
     };
     const [, sad] = Saider.saidify(_sad);
