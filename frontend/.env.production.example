# MATOU Frontend Environment Configuration (Production)
# Copy to .env.production for production builds
#
# For Electron builds: app.isPackaged will be true, using these values
# For web builds: these values are baked in at build time

# Production KERIA URLs
VITE_KERIA_ADMIN_URL=https://keria.example.com:3901
VITE_KERIA_BOOT_URL=https://keria.example.com:3903
VITE_KERIA_CESR_URL=https://keria.example.com:3902

# Production config server (or use backend endpoint)
VITE_CONFIG_SERVER_URL=https://config.example.com

# Backend API (Electron uses dynamic port, web uses this)
VITE_BACKEND_URL=https://api.example.com

# Production schema/witness URLs (external-facing)
VITE_SCHEMA_SERVER_URL=https://keria.example.com:7723
VITE_WITNESS_DOCKER_URL=https://witness.example.com:5642
